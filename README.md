## ~~Windows~~ Linux screensaver

A short ncurses program to get familiar with the library.

## Dependencies

`gcc`, `make`, `ncurses` and `gdb` (for debugging).

## Adding your own charset

Edit `snake.h` - just follow the pattern.


## License
